DROP DATABASE IF EXISTS `4um`;

CREATE DATABASE 4um;

-- USE 4um;

-- DROP TABLE IF EXISTS `User`;
		
-- CREATE TABLE `Users` (
--   `id` INTEGER AUTO_INCREMENT,
--   `username` VARCHAR(20) NOT NULL DEFAULT 'NULL',
--   `currentCurrency` INTEGER NULL DEFAULT NULL,
--   `totalCurrency` INTEGER NULL DEFAULT NULL,
--   `noviceRating` INTEGER NULL DEFAULT NULL,
--   `expertRating` INTEGER NULL DEFAULT NULL,
--   PRIMARY KEY (`id`)
-- );

-- DROP TABLE IF EXISTS `Questions`;
		
-- CREATE TABLE `Questions` (
--   `id` INTEGER AUTO_INCREMENT,
--   `questionTitle` VARCHAR(255) NOT NULL,
--   `questionBody` VARCHAR(500) NULL DEFAULT NULL,
--   `answer` VARCHAR(255) NULL DEFAULT NULL,
--   `Qid_User` INTEGER NOT NULL,
--   `Aid_User` INTEGER NULL DEFAULT NULL,
--   PRIMARY KEY (`id`)
-- );
