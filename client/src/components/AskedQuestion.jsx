import React from 'react';

const AskedQuestion = props => (
  <div>
    <h1 className="headline">Conditionally render css classes with Ng-If in Angular</h1>
    <p className="askedQuestionDescription">There seems to be a gap between the navbar and the rest of the page. I am trying to get rid of that but to no avail. have tried adjusting margins etc.</p> 
  </div>
);

export default AskedQuestion;